{
  "name": "api",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "dev": "nodemon --ext json,js --ignore node_modules --exec 'yarn start'",
    "start": "node index.js",
    "test": "jest --watchAll --verbose"
  },
  "devDependencies": {
    "jest": "^26.6.1",
    "nodemon": "^2.0.6",
    "supertest": "^6.0.0"
  },
  "dependencies": {
    "express": "^4.17.1"
  }
}
